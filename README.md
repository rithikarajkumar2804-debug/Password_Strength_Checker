# Password_Strength_Checker
Password Strength Checker is a Python program that evaluates password security using regular expressions. It checks length, uppercase, lowercase, numbers, and special characters to classify the password as Weak, Medium, or Strong. The project demonstrates the use of the re module and conditional logic.
